import { FileFormatePipe } from './file-formate.pipe';

describe('FileFormatePipe', () => {
  it('create an instance', () => {
    const pipe = new FileFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
